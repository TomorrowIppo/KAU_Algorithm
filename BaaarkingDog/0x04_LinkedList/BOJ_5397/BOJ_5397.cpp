#include <bits/stdc++.h>

using namespace std;

string str;
int T;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cin >> T;
    
    while(T--) {
        cin >> str;
        
        list<char> ls;
        
        for(auto c : ls) ls.push_back(c);
        auto cussor = ls.end();
    }
    
    
    return 0;
}
